# CLAUDE.md

Read `AGENTS.md` first. It is this repository's Desktop-specific agent contract.

Also read `../AGENTS.md` for workspace rules and cross-repo boundaries.

Use local repository files such as `package.json`, Electron main and renderer code, preload APIs, tests, scripts, and release config as the implementation source of truth.

Do not duplicate Desktop rules here. Update `AGENTS.md` when the contract changes.
